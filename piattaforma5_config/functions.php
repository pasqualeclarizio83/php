<?php

// Funzioni basate sul modello di Entità-Relazione (ER)
function getUtenti() {
    // Implementazione della funzione per ottenere gli utenti
    // Utilizza il modello ER per costruire la query
    $query = "SELECT * FROM utenti";
    
    // Esegui la query e restituisci i risultati
    // ...

    return $results;
}

// Aggiungi altre funzioni basate sul modello ER secondo necessità

?>
